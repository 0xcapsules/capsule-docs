{
  "name": "root",
  "private": true,
  "engines": {
    "node": "17.3.1"
  },
  "devDependencies": {
    "ts-node": "^10.5.0",
    "typescript": "^4.5.5"
  },
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "yarn workspaces foreach run build",
    "start": "cd packages/express-server && yarn start"
  },
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@3.4.1",
  "dependencies": {
    "@capsulecraft/serializer": "^0.1.4"
  }
}
